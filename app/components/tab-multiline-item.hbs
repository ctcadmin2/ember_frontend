{{! TODO fix no-nested-interactive/no-invalid-interactive }}
<div class="ui item">
  <div class="ui middle aligned grid">
    <div class="ui four wide column">
      {{t (concat "settings.main." (camelize @category))}}
    </div>
    <div class="ui twelve wide column">
      <div class="ui selection relaxed divided list">
        {{#each @list as |item|}}
          <MultilineItem @item={{item}} @deleteItem={{this.removeItem}} />
        {{/each}}
        <div class="ui item" role="button" {{on "click" this.addMode}}>
          {{#if this.editMode}}
            <div class="ui middle aligned grid">
              <div class="ui eight wide column">
                <div class="ui fluid input">
                  <Input type={{@type}} @value={{this.value}} />
                </div>
              </div>
              <div
                class="ui eight wide column"
                role="button"
                {{on "mouseenter" this.mouseIn}}
                {{on "mouseleave" this.mouseOut}}
                {{on "click" this.addItem}}
              >
                <div class="ui right floated">
                  {{#if this.showSaveIcon}}
                    <i class="large olive save icon"></i>
                  {{/if}}
                  {{t "general.save"}}
                </div>
              </div>
            </div>
          {{else}}
            <i class="left floated plus teal icon"></i>{{t "general.addNew"}}
          {{/if}}
        </div>
      </div>
    </div>
  </div>
</div>